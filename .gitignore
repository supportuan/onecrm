node_modules/
dist/
build/
.next/
.DS_Store
*.pem

# Env files — never commit secrets. Only the example template is allowed.
.env
.env.*
!.env.production.example
Frontend/node_modules/
Frontend/dist/
Frontend/.env
Backend/node_modules/
Backend/.env
Backend/.env.*
!Backend/.env.production.example

# Filled per-client runtime files (templates/examples stay tracked)
deploy/clients/*.env
!deploy/clients/_template.env

# Project handover pack (generated docs / local artefacts)
Project-Handover/
Project-Handover.zip
