/**
 * Retired: tenant isolation no longer applies. Each client is a separate install
 * (own database + env). See deploy/CLIENTS.md.
 */
console.log('[smoke] skipped — multi-tenancy has been removed.');
