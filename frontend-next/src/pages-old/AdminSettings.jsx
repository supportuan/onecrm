'use client';
import MinimalPage from './MinimalPage';

const AdminSettings = () => (
  <MinimalPage
    title="Admin & Settings"
    description="Configure application settings, permissions, and system controls."
  />
);

export default AdminSettings;

